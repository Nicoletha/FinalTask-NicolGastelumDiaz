/**
 * Shared timeout values used across page objects and wdio.conf.js.
 * Centralized here so a timing adjustment only needs to happen once.
 */
module.exports = {
    LOGIN_REDIRECT_TIMEOUT_MS: 15000,
    SORT_DROPDOWN_TIMEOUT_MS: 15000
};